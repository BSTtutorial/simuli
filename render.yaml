services:
  - type: web
    name: sagemath-calculator
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: COCALC_API_KEY
        sync: false
